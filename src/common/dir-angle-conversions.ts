export function cramangle2three(phiDegrees: number, thetaDegrees: number): number[]{

    // converts CRAM angle convention (in DEGREES) to ThreeJS angle convention (in RADIANS)
    // accounts for coordinate system shift 

    return [];


}