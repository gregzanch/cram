
import { Characters } from './constants';

export const Settings = {
  SHORT_CUT_KEYS: [
    Characters.COMMAND + " " + "Command",
    Characters.OPTION + " " + "Option",
    Characters.CONTROL + " " + "Control"
  ]
};

