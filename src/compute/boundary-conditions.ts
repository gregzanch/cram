export namespace BOUNDARY_CONDITION {
    export enum BEAM {
        
        FIXED_FIXED,
        FIXED_PINNED,
        FIXED_FREE,
        
        PINNED_FIXED,
        PINNED_PINNED,
        PINNED_FREE,
        
        FREE_FIXED,
        FREE_FREE,
        FREE_PINNED
        
    }
}
